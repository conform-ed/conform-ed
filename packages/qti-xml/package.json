{
  "name": "@conform-ed/qti-xml",
  "version": "0.0.0",
  "files": [
    "src",
    "dist"
  ],
  "type": "module",
  "module": "src/index.ts",
  "exports": {
    ".": {
      "development": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./node": {
      "development": "./src/node.ts",
      "types": "./dist/node.d.ts",
      "default": "./dist/node.js"
    }
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir dist --format esm --target browser --external @conform-ed/contracts --external fast-xml-parser --external fflate && bun build ./src/node.ts --outdir dist --format esm --target node --external @conform-ed/contracts --external fast-xml-parser --external fflate && bun run scripts/assert-bundle-boundaries.ts && tsc -p tsconfig.build.json",
    "format": "oxfmt --config ../../.oxfmtrc.jsonc --check .",
    "lint": "oxlint --config ../../.oxlintrc.jsonc .",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@conform-ed/contracts": "workspace:*",
    "fast-xml-parser": "^5.11.0",
    "fflate": "^0.8.3"
  },
  "devDependencies": {
    "xmllint-wasm": "^5.3.0"
  }
}
